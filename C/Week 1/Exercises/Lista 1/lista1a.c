#include<stdio.h>
void main(void){//mismo espaciado monospace, entramos a editar y preferencias, tipografías y colores, cambio de tamaño de letra.
	int m, n;/*otro comentario*/
	printf("Digite dos números enteros: ");
	scanf("%d %d", &m, &n);
	printf("%d + %d = %d", m, n, m+n);
}
